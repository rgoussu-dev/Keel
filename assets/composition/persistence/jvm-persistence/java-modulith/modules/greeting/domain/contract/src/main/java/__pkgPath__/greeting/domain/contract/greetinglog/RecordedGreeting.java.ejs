package <%= basePackage %>.greeting.domain.contract.greetinglog;

import java.time.Instant;

/** One greeting as the log recorded it. */
public record RecordedGreeting(long id, String name, Instant greetedAt) {}
