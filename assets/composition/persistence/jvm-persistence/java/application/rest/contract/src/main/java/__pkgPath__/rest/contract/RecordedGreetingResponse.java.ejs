package <%= basePackage %>.rest.contract;

import java.time.Instant;

/** Transport shape of one recorded greeting. */
public record RecordedGreetingResponse(long id, String name, Instant greetedAt) {}
