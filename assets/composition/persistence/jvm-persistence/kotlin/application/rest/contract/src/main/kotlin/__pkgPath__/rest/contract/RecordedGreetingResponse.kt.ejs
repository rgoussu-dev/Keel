package <%= basePackage %>.rest.contract

import java.time.Instant

/** Transport shape of one recorded greeting. */
data class RecordedGreetingResponse(val id: Long, val name: String, val greetedAt: Instant)
