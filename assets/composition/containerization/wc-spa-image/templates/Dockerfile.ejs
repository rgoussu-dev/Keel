# Thin image — no build stage. Serves the Vite bundle the host build
# produced; build it first:
#
#   <%= buildCommand %>
FROM nginx:alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY application/web-app/dist/ /usr/share/nginx/html/
EXPOSE 80
