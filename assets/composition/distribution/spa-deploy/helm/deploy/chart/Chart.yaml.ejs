apiVersion: v2
name: <%= projectName %>
description: Serves the SPA — the CI-built assets image populates a shared volume as an init container; stock nginx serves it.
type: application
version: 0.1.0
appVersion: '0.1.0'
