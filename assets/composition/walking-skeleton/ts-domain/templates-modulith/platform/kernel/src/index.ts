/**
 * The platform kernel's public surface: the dispatch vocabulary every
 * context is written against, plus the one implementation of it.
 */

export * from './kernel.ts';
export * from './registry-mediator.ts';
