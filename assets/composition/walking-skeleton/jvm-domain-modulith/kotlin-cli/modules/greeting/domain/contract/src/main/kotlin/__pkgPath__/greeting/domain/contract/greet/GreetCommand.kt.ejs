package <%= basePackage %>.greeting.domain.contract.greet

import <%= basePackage %>.platform.kernel.Command

/** Greet someone by name. The walking skeleton's only domain command. */
data class GreetCommand(val name: String) : Command<String>
