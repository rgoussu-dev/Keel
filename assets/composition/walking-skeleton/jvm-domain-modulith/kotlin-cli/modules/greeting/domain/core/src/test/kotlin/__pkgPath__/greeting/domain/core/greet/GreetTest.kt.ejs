package <%= basePackage %>.greeting.domain.core.greet

import org.junit.jupiter.api.Test

import <%= basePackage %>.greeting.domain.contract.greet.GreetCommand

import org.junit.jupiter.api.Assertions.assertEquals

class GreetTest {

    @Test
    fun greetsByName() {
        val handler = GreetHandler()

        val greeting = handler.handle(GreetCommand("Romain"))

        assertEquals("Hello, Romain!", greeting)
    }
}
