package <%= basePackage %>.platform.kernel;

/**
 * Marker for an intent expressed against the application core. The
 * type parameter {@code R} is the result the originating port
 * expects back after dispatch.
 */
public interface Command<R> {}
