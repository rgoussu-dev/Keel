package <%= basePackage %>.kernel

/**
 * Marker for an intent expressed against the application core. The
 * type parameter [R] is the result the originating port expects
 * back after dispatch.
 */
interface Command<R>
