package <%= basePackage %>.kernel;

/**
 * Dispatches commands to the handler that supports them. Primary
 * adapters (CLI, REST, batch, …) hold a Mediator reference and call
 * {@link #dispatch(Command)} instead of touching domain handlers
 * directly.
 */
public interface Mediator {
    <R> R dispatch(Command<R> command);
}
