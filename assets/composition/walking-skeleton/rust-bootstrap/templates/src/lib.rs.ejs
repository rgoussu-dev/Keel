//! <%= projectName %> — hexagonal walking skeleton scaffolded by
//! keel. `domain` is the contract face over a compiler-hidden core;
//! each `src/bin/` directory is one deployment unit wiring the
//! hexagon in its `main`.

pub mod domain;
