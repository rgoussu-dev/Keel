package <%= basePackage %>.guestbook.domain.contract.signing

/**
 * Someone who signs the guestbook. Guestbook's own value type — the
 * greeting module has never heard of it, and never will.
 */
data class Visitor(val name: String)
