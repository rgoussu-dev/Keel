package <%= basePackage %>.guestbook.domain.contract.signing

/** One line of the guestbook: who signed, and how they were welcomed. */
data class GuestbookEntry(val visitor: String, val welcome: String)
