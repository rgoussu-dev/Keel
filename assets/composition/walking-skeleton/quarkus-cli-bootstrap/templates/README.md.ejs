# <%= projectName %>

Quarkus CLI walking skeleton scaffolded by keel.

## Run

```sh
./gradlew --quiet --console=plain run --args="hello --name World"
```

## Test

```sh
./gradlew test
```

## Build a native binary

```sh
./gradlew build -Dquarkus.package.type=native
```
