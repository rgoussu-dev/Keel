{
  "name": "@<%= npmScope %>/web-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@<%= npmScope %>/design-system": "<%= workspaceDep %>",
    "@<%= npmScope %>/domain-api": "<%= workspaceDep %>",
    "@<%= npmScope %>/domain-core": "<%= workspaceDep %>"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
