package <%= basePackage %>.greeting.domain.contract;

import java.time.Instant;

/**
 * A view onto wall-clock time. Domain code never reads
 * {@link java.time.Instant#now()} directly — it depends on this port
 * so production wires a system clock and tests use a fake.
 */
public interface Clock {
    Instant now();
}
