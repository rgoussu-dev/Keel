package <%= basePackage %>.<%= module %>.domain.contract;

/**
 * What the <%= module %> context produced.
 *
 * @param detail human-readable outcome
 */
public record <%= Module %>Result(String detail) {}
