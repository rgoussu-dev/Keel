package <%= basePackage %>.<%= module %>.domain.contract

/** Why the <%= module %> context refused a [<%= Module %>Command]. */
class <%= Module %>Rejected(message: String) : RuntimeException(message)
