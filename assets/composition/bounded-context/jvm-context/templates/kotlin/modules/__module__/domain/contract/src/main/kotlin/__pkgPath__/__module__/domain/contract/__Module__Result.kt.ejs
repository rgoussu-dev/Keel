package <%= basePackage %>.<%= module %>.domain.contract

/**
 * What the <%= module %> context produced.
 *
 * @property detail human-readable outcome.
 */
data class <%= Module %>Result(val detail: String)
